diff --git a/RE4_JPN_RNG_SEARCH_V4_README.txt b/RE4_JPN_RNG_SEARCH_V4_README.txt
new file mode 100644
index 0000000..3d08fc9
--- /dev/null
+++ b/RE4_JPN_RNG_SEARCH_V4_README.txt
@@ -0,0 +1,24 @@
+RE4 JPN RNG Search v4 — internal in-memory searcher
+
+Target movie: neutral first-Ganado baseline (Leon runs forward, no drop).
+Automatic capture: movie frame 2818.
+Mutation window: even TAS slots 2822..2910.
+Budget: 5000 attempts.
+Target: $800D1EF0 r29 == 1.
+
+Mandatory self-checks before search:
+1) baseline gate $42B3
+2) release B at 2866 -> gate $47AC
+3) press L at 2864+2866 -> gate $D62C, route $BE42, final ID 2
+
+If any check fails, search aborts.
+
+Logs:
+  Logs/re4_jpn_rng_search_results.csv
+  Logs/re4_jpn_rng_search_FOUND.txt (only on success)
+
+Search phases:
+  validation -> 315 singles -> pairs of influential singles -> triples
+
+Mutation labels:
+  B_OFF, L, R, SX-8, SX+8, SY-8, SY+8
diff --git a/Source/Core/Core/CMakeLists.txt b/Source/Core/Core/CMakeLists.txt
index 21a656a..cce0cbe 100644
--- a/Source/Core/Core/CMakeLists.txt
+++ b/Source/Core/Core/CMakeLists.txt
@@ -500,6 +500,8 @@ add_library(core
   MemTools.h
   Movie.cpp
   Movie.h
+  RE4RNGSearch.cpp
+  RE4RNGSearch.h
   RE4RNGTrace.cpp
   RE4RNGTrace.h
   NetPlayClient.cpp
diff --git a/Source/Core/Core/Movie.cpp b/Source/Core/Core/Movie.cpp
index b760ef5..f33b9bd 100644
--- a/Source/Core/Core/Movie.cpp
+++ b/Source/Core/Core/Movie.cpp
@@ -58,6 +58,7 @@
 #include "Core/IOS/USB/Bluetooth/BTEmu.h"
 #include "Core/IOS/USB/Bluetooth/WiimoteDevice.h"
 #include "Core/NetPlayProto.h"
+#include "Core/RE4RNGSearch.h"
 #include "Core/RE4RNGTrace.h"
 #include "Core/State.h"
 #include "Core/System.h"
@@ -1138,6 +1139,7 @@ void MovieManager::PlayController(GCPadStatus* PadStatus, int controllerID)
   }
 
   memcpy(&m_pad_state, &m_temp_input[m_current_byte], sizeof(ControllerState));
+  Core::RE4RNGSearch::MutateMovieControllerState(m_current_frame, m_pad_state);
   m_current_byte += sizeof(ControllerState);
 
   PadStatus->isConnected = m_pad_state.is_connected;
diff --git a/Source/Core/Core/RE4RNGSearch.cpp b/Source/Core/Core/RE4RNGSearch.cpp
new file mode 100644
index 0000000..bf4aab6
--- /dev/null
+++ b/Source/Core/Core/RE4RNGSearch.cpp
@@ -0,0 +1,698 @@
+// Copyright 2026 Dolphin Emulator Project
+// SPDX-License-Identifier: GPL-2.0-or-later
+
+#include "Core/RE4RNGSearch.h"
+
+#include <algorithm>
+#include <atomic>
+#include <chrono>
+#include <deque>
+#include <fstream>
+#include <mutex>
+#include <set>
+#include <span>
+#include <string>
+#include <string_view>
+#include <tuple>
+#include <utility>
+#include <vector>
+
+#include <fmt/format.h>
+
+#include "Common/Config/Config.h"
+#include "Common/FileUtil.h"
+#include "Core/Config/MainSettings.h"
+#include "Core/HW/CPU.h"
+#include "Core/HW/Memmap.h"
+#include "Core/Movie.h"
+#include "Core/PowerPC/Gekko.h"
+#include "Core/PowerPC/PowerPC.h"
+#include "Core/RE4RNGTrace.h"
+#include "Core/State.h"
+#include "Core/System.h"
+#include "VideoCommon/OnScreenDisplay.h"
+
+namespace Core::RE4RNGSearch
+{
+namespace
+{
+constexpr u64 CAPTURE_FRAME = 2818;
+constexpr u64 WINDOW_FIRST = 2822;
+constexpr u64 WINDOW_LAST = 2910;
+constexpr u64 RESULT_FRAME = 2914;
+constexpr u64 HARD_END_FRAME = 2924;
+constexpr u64 BUDGET = 5000;
+constexpr u32 GATE_PC = 0x800D2794;
+constexpr u32 ROUTE_PC = 0x800D1E9C;
+constexpr u32 FINAL_ID_PC = 0x800D1EF0;
+constexpr u16 EXPECTED_BASELINE_GATE = 0x42B3;
